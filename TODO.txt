
	TODO, Not in any kind of order:

1. Fix bug in loadNew() line 111 in pluginManager.py, doesn't support files other than .py
2. Finish the plugin loading methods for non-python scripts in pluginManager.py
3. Maybe optimize the Print() method in iohandler.py
4. Create the update.py script to check for updates from github
6. Create a couple more plugins
7. Eventually add Windows support
9. Implement logger algorithm
10. Create cyclic gen plugin
11. Create cleanhands plugin (probably in Perl)
12. Create frame_mkr.py script
13. Bug: when setting multiple params in plugin context, only one shows after typing options
14.   File "/root/IntentLimit/resources/il_context.py", line 108, in loadTool
    for cmd_name, cmd_info in command_specs.items():
                              ^^^^^^^^^^^^^^^^^^^
AttributeError: 'NoneType' object has no attribute 'items'

^^ ADD CHECK FOR NO COMMANDS IN CONFIG

Coldheart:
	should have windows specific techniques:
		windows smb exploits:
			eternalblue:
			eternalromance:
			eternalsynergy:
			eternalchampion:
			educatedscholar:
		windows rdp/iis/system exploits:
			esteemaudit:
			explodingcan:
			emeraldthread:
		windows local privesc:
			epme:
			eclipsedwing:
			erraticgopher:

GoldenBullet:
	more generalized exploits:
		cisco/network exploits:
			extrabacon:
			epicbanana:
			jetplow:
			benigncertain:
			bananaglee:
			bananaballot:

CrazyIngrate:
	linux/unix exploits:
		nothing yet.
