test
	TODO, Not in any kind of order:

1. Fix bug in loadNew() line 111 in pluginManager.py, doesn't support files other than .py <- slightly fixed lol
2. Finish the plugin loading methods for non-python scripts in pluginManager.py
3. Maybe optimize the Print() method in iohandler.py
4. Create the update.py script to check for updates from github <- improve
9. Implement logger algorithm <- low priority
10. Create cyclic gen plugin
11. Create cleanhands plugin (probably in Perl)
12. Create frame_mkr.py script <- to streamline new tool creation
13. Need a way to better describe and or search exploits. these names arent explanatory.
	-> info list command added but need to clean it up and categorize the output
14. Search function will use a separate index.yaml that is populated at startup and contains metadata/tags 
	to ensure the search function is quick. Need to add tags field to each config.yaml with search terms 

15. FIX SEARCH. DOESNT SEARCH DEEP ENOUGH

Coldheart:
	should have windows specific techniques:
		windows smb exploits:
			eternalblue:
			eternalromance:
			eternalsynergy:
			eternalchampion:
			educatedscholar:
		windows rdp/iis/system exploits:
			esteemaudit:
			explodingcan:
			emeraldthread:
		windows local privesc:
			epme:
			eclipsedwing:
			erraticgopher:

GoldenBullet:
	more generalized exploits:
		cisco/network exploits:
			Rewrite in C?
			extrabacon:
			epicbanana:
			jetplow:
			benigncertain:
			bananaglee:
			bananaballot:

CrazyIngrate:
	linux/unix exploits:
		nothing yet.


GOAL END OF JUNE: Make IntentLimit packet-ready: README screenshots, architecture diagram, install docs, examples, plugin docs, tests, demo GIF.
