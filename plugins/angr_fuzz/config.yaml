name: angr_fuzz
description: Small angr script for reversing
type: Plugin
version: 1.0
params:
 - binpath # path to binary
 - solve   # address of solve condition (use ghidra or other 3rd party tools)
 - avoid   # address of fail condition
 - base    # address where code starts
 - flagsize # estimate size of flag
